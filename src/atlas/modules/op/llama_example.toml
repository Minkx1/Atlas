[llama]

# Path to the LLM model in GGUF format.
# You can download any compatible GGUF model from Hugging Face
# or another source and put it in '$DATA/models/llm_models/'.
model_path = "models/llm_models/Llama-3.2-3B-Instruct-Q5_K_M.gguf"

# Initial system prompt passed to the LLM when a conversation starts.
# This defines the assistant's identity, the way it addresses the user,
# and the general format/style of its responses.
initial_prompt = """You are Atlas, a highly efficient AI voice assistant.
Your absolute priority is to provide plain, spoken-language responses.

Rules:
1. Always call the user "Sir".
2. Respond strictly in English.
3. Use ONLY plain text (letters, numbers, and basic punctuation). 
4. Keep answers concise, limited to 2-5 sentences.
"""

# Maximum amount of conversation context kept by the LLM, measured in tokens.
# A larger value allows the model to remember more previous messages,
# but requires more RAM/VRAM and can make inference slower.
context_tokens = 2048

# Maximum number of tokens the LLM can generate for a single response.
# Higher values allow longer answers but increase generation time.
max_msg_tokens = 128

# Controls how random/creative the LLM responses are.
# Lower values make responses more predictable and consistent.
# Higher values make responses more varied but potentially less reliable.
temperature = 0.7
